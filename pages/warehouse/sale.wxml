<!--pages/warehouse/sale.wxml-->
<view class="container">
  <view class="warehouse-sale">
    <h4> {{title}} </h4>
    <h3> {{subTitle}} </h3>
    <p>{{blurb}}</p>
    <span bindtap="shopTheSale">{{link}}</span>
    <p class="small-font">{{finePrint}}</p>
  </view>
</view>
