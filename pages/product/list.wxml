<!--pages/product/list.wxml-->
<view class="container">
  <div class="flex-grid-duo" wx:for="{{products}}" wx:key="code">
        <div class="col"><div class="row code" bindtap="getProductDetail" data-pc9="{{item.code}}">{{item.name}}</div></div>
  </div>
</view>

